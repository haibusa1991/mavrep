package com.personal.microart.core.auth.base;

/**
 * This enum represents the different types of authentication that the application supports.
 */
public enum AuthenticationType {
    BASIC,
    JWT
}
